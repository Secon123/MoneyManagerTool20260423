# BUG-003：添加收支接口返回 403

**发现人**：曾昌缙  
**发现时间**：2026-05-05 14:10  
**状态**：✅ 已修复并验证通过

## 问题描述
使用登录获取的 token 调用 `POST /api/transactions`，返回 403。

## 原因（后端确认）
拦截器只放行了 `/api/auth/**`，但拦截了 `/api/**` 下的业务接口。

## 验证结果
修复后，✅ 添加支出成功（200）
